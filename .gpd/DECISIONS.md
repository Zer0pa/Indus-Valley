# Decisions

Append-only decision log.

| Date | Phase | Decision | Rationale | Rollback Trigger | Status |
| ---- | ----- | -------- | --------- | ---------------- | ------ |
| `2026-04-23` | `00` | Keep `gnosis-indus` as an anchor application and evidence repo | corrected workstream set ranks it as the evidence-bearing anchor, not the lead thesis | new control-plane evidence demotes the lane further | `RATIFIED` |
| `2026-04-23` | `00` | Keep search-without-decode inside this repo | current evidence supports an application surface, not a sovereign repo | a later evidence program proves a clean independent moat | `RATIFIED` |
| `2026-04-23` | `00` | Preserve the Phase 4 stability caveat in all front-door docs | packaging cannot outrun the authority docs | a stronger authority source explicitly closes the caveat | `RATIFIED` |
| `2026-04-24` | `01` | First extraction slice chosen: `scripts/indus/phase4_search_demo.py` → `src/gnosis_indus/search_surface/` | lowest rights-gate risk (derived cluster IDs only, no images), smallest helper-dependency footprint (catalogue JSON plus query list), cleanest stronger-smoke-path story (instantiable query engine with Track C latency and compression gates); Phase 4 catalogue and Phase 5 falsification families sequenced as explicit second and third waves | Phase 02 extraction uncovers a dependency on a rights-gated asset family that makes the slice unshippable | `RATIFIED` |
| `2026-04-24` | `02` | Pivot first slice from literal extraction to clean-room reimplementation anchored to `authority/review_pack/search_demo_summary.md` | original `scripts/indus/phase4_search_demo.py` not accessible from this machine or pod (`mdfind` empty; `/workspace` empty; original monorepo root `<MONOREPO_ROOT>` absent); the authority doc carries a complete functional spec (API names, latency/compression gates, 10 query ground-truth records, partial cluster memberships) sufficient for a clean-room implementation that is strictly more standalone than a copy that drags in monorepo helpers (per `AGENTS.md` "more standalone, not less truthful") | a future evidence run produces query results that the clean-room engine cannot reproduce on the demo fixture | `RATIFIED` |
| `2026-04-24` | `03` | Truth-surface coherence audit passed; PRD §8 promotion blockers split into 2 closed (extracted runtime → `src/gnosis_indus/search_surface/`; clean-machine replay → `pip install -e .[test] && pytest -q`, 14 passed, RunPod-confirmed) and 2 currently open (image rights, owner-deferred license/contact) | Phase 02 landed a real runtime slice and a real stronger smoke path, so the front door, authority snapshot, PRD §8, and TODO had to agree with that reality without inflating it; the closed-vs-open split preserves the carried-forward language of all four blockers and surfaces the two remaining owner-bound items honestly; no PRD §1.3 falsification condition triggered (k=70 caveat preserved, no decipherment language, search not reframed as sovereign repo, no widened image rights) | a later finding shows that `src/gnosis_indus/search_surface/` does not in fact reproduce the authority-doc queries on a clean machine, OR a new control-plane source widens or reframes the closed blockers | `RATIFIED` |
