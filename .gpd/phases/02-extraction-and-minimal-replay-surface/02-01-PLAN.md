---
phase: 02-extraction-and-minimal-replay-surface
plan: 01
type: execute
wave: 1
depends_on:
  - 01-source-truth-and-authority-admission/01
files_modified:
  - src/gnosis_indus/search_surface/__init__.py
  - src/gnosis_indus/search_surface/catalogue.py
  - src/gnosis_indus/search_surface/engine.py
  - src/gnosis_indus/search_surface/_fixture.py
  - artifacts/phase4/indus_catalogue_demo_fixture.json
  - artifacts/phase4/README.md
  - tests/__init__.py
  - tests/test_search_surface.py
  - pyproject.toml
  - SMOKE_TESTS.md
  - .gpd/STATE.md
  - .gpd/state.json
  - .gpd/ROADMAP.md
  - .gpd/REQUIREMENTS.md
  - .gpd/DECISIONS.md
interactive: false

conventions:
  units: "n/a"
  coordinates: "not applicable"
  reporting_policy: "No interim reporting unless there is a real blocker."
  truth_policy: "Runtime truth and artifact truth outrank prose. Implementation is anchored to the admitted Phase 4 Track C authority doc; every reproducible number is traceable to that doc."

contract:
  schema_version: 1
  scope:
    question: "Can the Phase 4 search-without-decode application surface be cleanly re-implemented from the admitted authority documents, producing a stronger smoke path than the starter import check and reproducing authority-doc query results on an authority-anchored demo fixture?"
  claims:
    - id: claim-search-surface-runtime
      statement: "`src/gnosis_indus/search_surface/` exposes a `SearchEngine` with `sign_lookup`, `sequence_search`, and `subsequence_search` APIs whose behaviour on the admitted demo fixture matches the ground-truth query records in `search_demo_summary.md` (modulo '... and N more' truncation)."
      deliverables: [deliv-search-surface, deliv-demo-fixture, deliv-stronger-smoke]
      acceptance_tests: [test-api-shape, test-query-reproduction, test-latency-gate, test-caveat-visibility]
      references: [ref-phase4-track-c, ref-phase4, ref-phase5, ref-authority, ref-prd]
  deliverables:
    - id: deliv-search-surface
      kind: code
      path: src/gnosis_indus/search_surface/
      description: Python package with SearchEngine, Catalogue loader, and fixture helper.
    - id: deliv-demo-fixture
      kind: data
      path: artifacts/phase4/indus_catalogue_demo_fixture.json
      description: Authority-anchored demo catalogue JSON assembled strictly from the data present in `search_demo_summary.md`. Labelled as a demo fixture, not the real full catalogue.
    - id: deliv-stronger-smoke
      kind: test
      path: tests/test_search_surface.py
      description: pytest suite reproducing authority-doc query results on the demo fixture and asserting the <100 ms latency gate on the largest query.
  references:
    - id: ref-prd
      kind: document
      locator: PRD_GNOSIS_INDUS_ANCHOR_APPLICATION.md
      role: scope
    - id: ref-authority
      kind: document
      locator: AUTHORITY_SNAPSHOT.md
      role: authority
    - id: ref-phase4
      kind: document
      locator: authority/review_pack/phase4_governing_verdict.md
      role: morphology-caveat
    - id: ref-phase4-track-c
      kind: document
      locator: authority/review_pack/search_demo_summary.md
      role: functional-spec-and-ground-truth
    - id: ref-phase5
      kind: document
      locator: authority/review_pack/phase5_governing_verdict.md
      role: non-decipherment
  acceptance_tests:
    - id: test-api-shape
      subject: claim-search-surface-runtime
      kind: code
      procedure: "Instantiate `SearchEngine` from the demo-fixture JSON. Confirm `sign_lookup`, `sequence_search`, and `subsequence_search` exist and return the documented fields (cluster_id, cluster_size, members, dominant_set, dominant_graph; match counts and cluster-sequence lists)."
      pass_condition: "pytest passes."
      evidence_required: [deliv-search-surface, deliv-stronger-smoke]
    - id: test-query-reproduction
      subject: claim-search-surface-runtime
      kind: numerical
      procedure: "Run queries 1, 2, 3 (sign_lookup for signs 324, 122, 385), query 6 (sequence_search [65 38]), query 9 (subsequence_search [26 57 65]), and query 10 (subsequence_search [11 15 40]) from `search_demo_summary.md`. Assert that each result matches the authority-doc values on the fixture (member lists, cluster sizes, dominant-set/graph labels where fixture covers them; match counts are asserted only for the inscriptions explicitly enumerated in the doc, with a comment acknowledging the '... and N more' truncation)."
      pass_condition: "All asserted queries match authority doc on fixture."
      evidence_required: [deliv-search-surface, deliv-demo-fixture, deliv-stronger-smoke]
    - id: test-latency-gate
      subject: claim-search-surface-runtime
      kind: numerical
      procedure: "Measure `sequence_search([65, 38])` latency on the fixture. Assert < 100 ms (the authority-doc gate). Use `time.perf_counter`, median of 5 runs."
      pass_condition: "Median latency < 100 ms."
      evidence_required: [deliv-search-surface, deliv-stronger-smoke]
    - id: test-caveat-visibility
      subject: claim-search-surface-runtime
      kind: audit
      procedure: "Grep the docstrings of `SearchEngine`, `Catalogue`, and the demo fixture README for explicit carry-forward of (a) the k=70 conditional caveat, (b) the non-decipherment posture, (c) the demo-fixture / full-catalogue distinction."
      pass_condition: "All three caveats appear at least once in the package's user-facing surface."
      evidence_required: [deliv-search-surface, deliv-demo-fixture]
---

<objective>
Land the first real runtime slice under `src/gnosis_indus/search_surface/`
as a clean-room implementation anchored to the admitted Phase 4 Track C
authority document, and stand up a stronger smoke path that reproduces
authority-doc query results on an authority-anchored demo fixture.
</objective>

<context>
@PRD_GNOSIS_INDUS_ANCHOR_APPLICATION.md
@AUTHORITY_SNAPSHOT.md
@SOURCE_BOUNDARY.md
@DATA_POLICY.md
@MIGRATION_PLAN.md
@SMOKE_TESTS.md
@docs/family/INDUS_EXPORT_CONTRACT.md
@authority/review_pack/search_demo_summary.md
@authority/review_pack/phase4_governing_verdict.md
@authority/review_pack/phase5_governing_verdict.md
@.gpd/phases/02-extraction-and-minimal-replay-surface/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble authority-anchored demo fixture</name>
  <files>artifacts/phase4/indus_catalogue_demo_fixture.json, artifacts/phase4/README.md</files>
  <action>From `authority/review_pack/search_demo_summary.md`, extract every fact and build a JSON fixture with the schema declared in the plan. Include: (a) cluster memberships for clusters 2, 26, 38, 65 (explicit in queries 1, 2, 3, 5); (b) inscriptions M-1A through M-9A with both raw and cluster sequences (Sample Encodings table); (c) every additional inscription mentioned with a cluster sequence in query 6–10 results (M-14A, M-15A, M-19A, M-20A, M-21A, M-28A, M-30A, M-36A, M-38A, M-44A, M-49A, M-50A, M-54A, M-64A, M-86A, M-91A, M-117A, M-122A, M-135A). Mark every unmapped token with -1 as the doc does. Write `artifacts/phase4/README.md` explaining that this is an authority-backed demo fixture, not the full k=70 catalogue (which stays `FETCH_EXTERNAL` per DATA_POLICY), and listing the exact authority-doc lines that each fixture row traces to.</action>
  <verify>Every fixture row has a citation back to a line in `search_demo_summary.md`. Nothing is fabricated.</verify>
  <done>The fixture is sufficient to reproduce queries 1, 2, 3, 6, 9, 10 from the authority doc.</done>
</task>

<task type="auto">
  <name>Task 2: Implement search_surface package</name>
  <files>src/gnosis_indus/search_surface/__init__.py, src/gnosis_indus/search_surface/catalogue.py, src/gnosis_indus/search_surface/engine.py, src/gnosis_indus/search_surface/_fixture.py</files>
  <action>Write a minimal, dependency-light implementation. `Catalogue` loads the JSON fixture (or any other catalogue matching the schema) into frozen dataclasses. `SearchEngine` exposes `sign_lookup(sign_id) -> SignLookupResult`, `sequence_search(cluster_seq) -> SequenceSearchResult`, `subsequence_search(cluster_seq) -> SequenceSearchResult`. `sequence_search` matches the query as a contiguous sub-sequence anywhere in each inscription's cluster sequence (this is what query 6's matches imply); `subsequence_search` matches the query as a (not necessarily contiguous) sub-sequence preserving order (this is what query 9/10's matches imply — verify by tracing the query cluster sequence through the matched inscription sequences). `_fixture.py` exposes `load_demo_fixture() -> SearchEngine` as a one-call convenience. All public types carry docstrings that cite `authority/review_pack/search_demo_summary.md` and preserve the Phase 4 k=70 conditional caveat and the non-decipherment posture.</action>
  <verify>Package imports cleanly under Python 3.11. No dependencies outside stdlib + numpy (numpy optional, used only for potential later latency tuning).</verify>
  <done>`python -c "from gnosis_indus.search_surface import load_demo_fixture; e = load_demo_fixture(); print(e.sign_lookup(324))"` works.</done>
</task>

<task type="auto">
  <name>Task 3: Stronger smoke path + pytest</name>
  <files>tests/__init__.py, tests/test_search_surface.py, pyproject.toml, SMOKE_TESTS.md</files>
  <action>Write a pytest suite that asserts the four acceptance tests. `test_api_shape` instantiates via `load_demo_fixture` and checks return-value schemas. `test_query_reproduction` reproduces queries 1, 2, 3, 6, 9, 10 against the fixture, comparing to authority-doc values. `test_latency_gate` measures `sequence_search([65, 38])` median over 5 runs, asserts < 100 ms. `test_caveat_visibility` greps package docstrings for the three required caveats. Update `pyproject.toml` to add `numpy>=1.24` (optional) and a `[project.optional-dependencies] test = ["pytest>=8"]` section. Update `SMOKE_TESTS.md` — remove the aspirational Phase 02 target section and replace it with the now-real command: `pip install -e .[test] && pytest -q`.</action>
  <verify>`pytest -q` passes on Python 3.11.</verify>
  <done>The stronger smoke path is concrete and runs green locally.</done>
</task>

<task type="auto">
  <name>Task 4: Advance GPD state + decision log</name>
  <files>.gpd/STATE.md, .gpd/state.json, .gpd/ROADMAP.md, .gpd/REQUIREMENTS.md, .gpd/DECISIONS.md</files>
  <action>Update `STATE.md` and `state.json` to reflect Phase 02 Plan 01 complete; update progress bar to 80%. In `ROADMAP.md`, flip Phase 02 row to `1/1 Complete (2026-04-24)`. In `REQUIREMENTS.md`, flip `CALC-02` already [x] stays [x]; flip `VALD-04` from [ ] to [x] with a note referencing `tests/test_search_surface.py`. In `DECISIONS.md`, append a row recording the clean-room pivot: source inaccessible, authority doc sufficient, no monorepo helpers dragged in.</action>
  <verify>`STATE.md` and `state.json` agree. All requirement checkboxes align with the deliverables.</verify>
  <done>GPD control plane is coherent and ready for Phase 03.</done>
</task>

</tasks>
